ignore:
  field_paths:
    - CreateLoadBalancerInput.Type
    # priority has to be implementend manually due to a type mismatch in
    # autogenerated code
    - CreateRuleInput.Priority
resources:
  LoadBalancer:
    exceptions:
      errors:
        404:
          code: LoadBalancerNotFound
  TargetGroup:
    exceptions:
      errors:
        404:
          code: TargetGroupNotFound
